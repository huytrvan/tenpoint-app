import { StyleSheet } from 'react-native';
import { textAreaStyle } from '../../styles/common.style';

export default StyleSheet.create({
  textArea: {
    ...textAreaStyle,
    height: 400,
  }
});